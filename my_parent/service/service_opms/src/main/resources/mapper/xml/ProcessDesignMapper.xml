<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccd.opms.mapper.ProcessDesignMapper">


    <select id="findPage" resultType="com.ccd.opms.entity.ProcessDesign">

        SELECT
        *
        FROM
        opms_process_design pd inner join opms_design_data dd
        on
        pd.design_data_id = dd.design_data_id
        where ${ew.customSqlSegment}

    </select>


</mapper>
